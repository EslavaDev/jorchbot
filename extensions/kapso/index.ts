/**
 * @module extensions/kapso
 * @phase 1
 * @description WhatsApp channel plugin via Kapso.ai API (Plugin SDK)
 * @status placeholder
 */

// This module will be implemented in Phase 1 as a channel plugin.
// Uses OpenClaw Plugin SDK — inherits DM pairing, chunking, access control.
// Pattern: follows extensions/whatsapp/ structure.
// See: docs/phase-1-single-session.md

// import type { OpenClawPluginApi } from "openclaw/plugin-sdk";
// import { emptyPluginConfigSchema } from "openclaw/plugin-sdk";

// const plugin = {
//   id: "kapso",
//   name: "Kapso (WhatsApp)",
//   description: "WhatsApp channel plugin via Kapso.ai API",
//   configSchema: emptyPluginConfigSchema(),
//   register(api: OpenClawPluginApi) {
//     api.registerChannel({ plugin: kapsoPlugin });
//   },
// };
//
// export default plugin;

export type KapsoPluginPlaceholder = Record<string, never>;
