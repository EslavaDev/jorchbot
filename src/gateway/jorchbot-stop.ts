/**
 * Stop the JorchBot gateway.
 *
 * Phase 0: Stub implementation — reports that no gateway is running.
 * Full implementation with PID file / port detection comes in later phases.
 */
export async function stopGateway(): Promise<void> {
  console.log("[jorchbot] no running gateway found");
}
