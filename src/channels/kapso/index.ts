// Phase 1: Kapso WhatsApp channel
// NOTE (DeepWiki rev.2): Implement as OpenClaw channel plugin in extensions/kapso/
// using the Plugin SDK pattern (see extensions/whatsapp/ for reference).
// This placeholder stays for now but the real implementation goes in extensions/.
export type KapsoChannelPlaceholder = Record<string, never>;
